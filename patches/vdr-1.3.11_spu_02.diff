diff -ur vdr-1.3.11_orig/dvbosd.c vdr-1.3.11/dvbosd.c
--- vdr-1.3.11_orig/dvbosd.c	Tue Jun 22 17:55:06 2004
+++ vdr-1.3.11/dvbosd.c	Wed Jul  7 12:00:56 2004
@@ -39,6 +39,10 @@
   shown = false;
   if (osdDev < 0)
      esyslog("ERROR: illegal OSD device handle (%d)!", osdDev);
+  for (int i = 1 ;i <= MAXNUMWINDOWS; i++) {
+     Cmd(OSD_SetWindow, 0, i);
+     Cmd(OSD_Clear);
+     }
 }
 
 cDvbOsd::~cDvbOsd()
diff -ur vdr-1.3.11_orig/dvbspu.c vdr-1.3.11/dvbspu.c
--- vdr-1.3.11_orig/dvbspu.c	Tue Jun 22 17:55:06 2004
+++ vdr-1.3.11/dvbspu.c	Wed Jul  7 12:22:48 2004
@@ -236,9 +236,9 @@
     delete osd;
 }
 
-void cDvbSpuDecoder::processSPU(uint32_t pts, uint8_t * buf)
+void cDvbSpuDecoder::processSPU(uint32_t pts, uint8_t * buf, bool allowedShow)
 {
-    setTime(pts);
+    setTime(pts, allowedShow);
 
     DEBUG("SPU pushData: pts: %d\n", pts);
 
@@ -403,6 +403,11 @@
 
 int cDvbSpuDecoder::setTime(uint32_t pts)
 {
+   return setTime(pts, true);
+}
+
+int cDvbSpuDecoder::setTime(uint32_t pts, bool allowedShow)
+{
     if (!spu)
         return 0;
 
@@ -503,7 +508,7 @@
         } else if (!clean)
             state = spSHOW;
 
-        if (state == spSHOW || state == spMENU)
+        if ((state == spSHOW && allowedShow) || state == spMENU)
             Draw();
 
         if (state == spHIDE)
diff -ur vdr-1.3.11_orig/dvbspu.h vdr-1.3.11/dvbspu.h
--- vdr-1.3.11_orig/dvbspu.h	Tue Jun 22 17:55:06 2004
+++ vdr-1.3.11/dvbspu.h	Wed Jul  7 11:55:51 2004
@@ -131,6 +131,7 @@
     int ScaleYcoord(int value);
     int ScaleYres(int value);
     void DrawBmp(sDvbSpuRect & size, cBitmap * bmp);
+    int setTime(uint32_t pts, bool allowedShow);
 
   public:
     cDvbSpuDecoder();
@@ -147,7 +148,7 @@
     void Hide(void);
     void Draw(void);
     bool IsVisible(void) { return osd != NULL; }
-    void processSPU(uint32_t pts, uint8_t * buf);
+    void processSPU(uint32_t pts, uint8_t * buf, bool allowedShow);
 };
 
 // --- cDvbSpuPalette -------------------------------------------
diff -ur vdr-1.3.11_orig/spu.h vdr-1.3.11/spu.h
--- vdr-1.3.11_orig/spu.h	Tue Jun 22 17:55:06 2004
+++ vdr-1.3.11/spu.h	Wed Jul  7 11:55:19 2004
@@ -35,7 +35,7 @@
     virtual void Hide(void) = 0;
     virtual void Draw(void) = 0;
     virtual bool IsVisible(void) = 0;
-    virtual void processSPU(uint32_t pts, uint8_t * buf) = 0;
+    virtual void processSPU(uint32_t pts, uint8_t * buf, bool allowedShow) = 0;
 };
 
 #endif                          // __SPU_VDR_H
